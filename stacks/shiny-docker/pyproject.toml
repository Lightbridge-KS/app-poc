[project]
name = "shiny-docker-poc"
version = "0.1.0"
description = "Proof that a Shiny for Python app runs under Docker Compose"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "palmerpenguins>=0.1.6",
    "plotnine>=0.15.7",
    "polars>=1.43.2",
    "shiny>=1.7.0",
]
